import { createReducer, createAction, current, PayloadAction, nanoid } from '@reduxjs/toolkit'

const initalState = {

}

const employee = createReducer(initalState, (builder) => {

})

export default employee;